pipeline {
    agent any

    stages {
        stage('STAGE 00') {
            steps {
                echo 'Pipeline usando jenkinsfile'
            }
        }

        stage('stage 01') {
            steps {
                echo 'Pipeline step 2'
            }
        }
    }
}